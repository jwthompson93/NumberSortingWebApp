@model CreateViewModel
@{
    ViewData["Title"] = "Create New Sort Job";
}

@if (TempData.ContainsKey("alert"))
{
    <div class="alert alert-@Model.Alert.Type.ToString()" role="alert">
        @Model.Alert.Message
    </div>
}
<div class="text-left">
    <form method="post" asp-action="Create">
        <table class="table">
            <tr>
                <td>Current Array</td>
                <td>
                    <label asp-for="NumbersInputArray" />
                </td>
                <td> </td>
            </tr>
            <tr>
                <td>Input Number to be added</td>
                <td>
                    <input type="number" class="form-control" asp-for="NumberInput" required/>
                    <div class="valid-feedback"></div>
                </td>
                <td>
                    <button onclick="@(() => Model.NumberInput_onClick)" >Add</button>
                </td>
            </tr>
            <tr>
                <td>Sort Order</td>
                <td>
                    <select asp-for="SortDirection">
                        <option value="0">Descending</option>
                        <option value="1">Ascending</option>
                    </select>
                </td>
                <td> </td>
            </tr>
        </table>
        <div style="text-align:left">
            <!--<a asp-area="" asp-controller="NumberSorting" asp-action="Create" class="btn btn-primary btn-sm">Create</a>-->
            <input type="submit" />
        </div>
    </form>
</div>
